language: bash

services:
  - docker

env:
  global:
    - PATH=$HOME/.local/bin:$PATH

before_install:
  - cp $TRAVIS_BUILD_DIR/build.sh $HOME/.local/bin
  - build.sh install-terraform

env:
  - JOB="docker"
  - JOB="terraform"

script: build.sh "$JOB"

notifications:
  email: false
