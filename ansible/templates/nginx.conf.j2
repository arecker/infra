events {}

http {
    {% for name, port in variables.items() %}
    server {
        listen 80;
        server_name {{ name }};

        location / {
            proxy_pass http://127.0.0.1:{{ port }};
        }
    }
    {% endfor %}
}
