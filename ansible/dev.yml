---
- "hosts": "dev.local"
  "name": "dev server"
  "remote_user": "alex"
  "tasks":
  - "become": true
    "name": "package: {{ item }}"
    "package":
      "name": "{{ item }}"
      "state": "present"
    "with_items":
    - "git"
    - "nfs-common"
    - "python3"
    - "python3-pip"
    - "python3-setuptools"
    - "python3-venv"
  - "file":
      "mode": "0751"
      "path": "{{ item }}"
      "state": "directory"
    "name": "directory: {{ item }}"
    "with_items":
    - "~/.config/systemd/user/"
    - "~/bin"
    - "~/envs"
    - "~/mnt"
    - "~/src"
    - "~/venvs"
  - "become": true
    "mount":
      "fstype": "nfs"
      "path": "/home/alex/mnt"
      "src": "nas.local:/volume1/dev"
      "state": "present"
    "name": "mount: /home/alex/mnt"
...

